# runtime log written by the server
revo_server.log

# local Claude Code settings
.claude/

# obj2step Python virtual environment (~600 MB; recreate via requirements.txt)
files/.venv/

# Python caches
__pycache__/
*.pyc

# generated converter output (STEP/STL) and the raw archive
OBJ_examples/out/
files.zip

# personal design scratch + debug artifacts in the repo root (not part of the tool)
/tinker (*
/fillet_error*.png
